pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // allows us to get code from other resources
        maven { url "https://jitpack.io" }
    }
}

rootProject.name = "ImdbMovieSearch2023a"
include ':app'
